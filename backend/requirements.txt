# Kramly Backend — Python dependencies
# Install: pip install -r requirements.txt

# --- Core ---
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.13.4
pydantic-settings==2.14.2

# --- Database ---
neo4j==6.2.0

# --- Utilities ---
python-dotenv==1.0.1
httpx>=0.27.0   # used directly for Groq/Mistral chat-completion calls (agent/llm_client.py)
apscheduler==3.11.3   # autonomous background scheduler (decay scan + crowd-confidence rescan, main.py)

# --- Testing (dev only) ---
pytest==9.1.1
